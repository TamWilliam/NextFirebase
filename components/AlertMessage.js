/* import React from "react";

const AlertMessage = ({ type, children }) => {

  const alertClasses = {
    success: "bg-green-500 text-white",
    error: "bg-red-500 text-white",
  };

  return (
    <div className={`p-3 rounded-md mb-4 ${alertClasses[type]}`}>
      {children}
    </div>
  );
};

export default AlertMessage;
 */